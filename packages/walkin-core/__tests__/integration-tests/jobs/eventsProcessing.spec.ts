import {} from "../../../src/jobs/eventsProcessing";
describe.skip("Events Processing Job test", () => {
  test("should Process an event", async () => {
    // processEvents({ data }, () => {});
  });
});
